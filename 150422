//Добавил 100 пользователей 
SELECT company, 
COUNT(ID) AS 'to work >10' FROM users GROUP BY company HAVING COUNT(ID) > 10;
SELECT company, 
COUNT(ID) AS 'to work 15+-20 devolopers' FROM users GROUP BY company HAVING COUNT(ID) BETWEEN 15 AND 20;
